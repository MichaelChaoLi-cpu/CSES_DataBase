# Resources

Source code and reusable project resources are tracked by Git.

- `cses_db/`: inventory, harmonization, build, validation, backup verification, SQL rendering, and
  migration planning/validation.
- `specs/`: reviewed schema and object-ownership contracts.
- `sql/`: reviewed schema DDL and generated transactional migration SQL.
- `tests/`: automated tests for repository contracts and reusable code.
